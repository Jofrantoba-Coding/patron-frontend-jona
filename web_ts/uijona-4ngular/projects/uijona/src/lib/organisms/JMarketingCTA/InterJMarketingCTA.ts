/** Contrato publico de JMarketingCTA. */
export interface InterJMarketingCTA {
  heading: string;
  description?: string;
  primaryLabel: string;
  primaryHref?: string;
  secondaryLabel?: string;
  secondaryHref?: string;
}
