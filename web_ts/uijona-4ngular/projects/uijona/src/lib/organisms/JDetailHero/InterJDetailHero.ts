/** Contrato publico de JDetailHero. Slot `[jVisual]`. */
export interface InterJDetailHero {
  backHref: string;
  backLabel: string;
  eyebrow?: string;
  title: string;
  outcome: string;
  primaryHref: string;
  primaryLabel: string;
  secondaryHref?: string;
  secondaryLabel?: string;
}
