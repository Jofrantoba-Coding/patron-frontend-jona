// InterUiHomeSession.ts — Level 3: Organism / JONA Layer: Interface (Contract)
// Pure contract for the authenticated session home organism.
export interface InterUiHomeSession {
  logout: () => void;
}
