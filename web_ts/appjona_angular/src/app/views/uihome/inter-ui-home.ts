// inter-ui-home.ts
// Capa: Interface (contrato puro) para la vista Home (login page)
// Restricciones: sin lógica, sin estado, sin imports externos
export interface InterUiHome {
  onMount: () => void;
}
